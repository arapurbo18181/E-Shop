import { LoginCom } from "../../components";
import { MainLayout } from "../../layouts";

const Login = () => {
  return (
    <MainLayout>
      <LoginCom />
    </MainLayout>
  );
};

export default Login;
